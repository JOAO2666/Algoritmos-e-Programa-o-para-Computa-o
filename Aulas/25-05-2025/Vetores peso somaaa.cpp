#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main() {
		float peso [6];
		peso [0] = 53.3;
		peso [1] = 72.4;
		peso [2] = 71.8;
		peso [3] = 52.9;
	    peso [4] = 96.5;
	    peso [5] = 48.8;
	return 0;
}
